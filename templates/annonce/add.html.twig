{% extends 'TemplateDashboard.html.twig' %}

{% block title %}Ajout annonce{% endblock %}

{% block body %}

        <div >
            {{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn-success">Créer </button>
            {{ form_end(form) }}
        </div>
{% endblock %}
